require 'spec_helper'

describe ActiveSms do
  it 'has a version number' do
    expect(ActiveSms::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
